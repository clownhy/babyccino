{
  "name": "babyccino",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "deploy-all": "git add . && git commit -m \"更新代码\" && git push origin main && npm run build && echo \"babyccino.coffee\" > dist/CNAME && git add dist -f && git commit -m \"更新网站\" && git subtree push --prefix dist origin gh-pages"
  },
  "homepage": "https://clownhy.github.io/babyccino/",
  "dependencies": {
    "vue": "^3.5.0",
    "vue-router": "^4.5.0",
    "@fortawesome/fontawesome-free": "^6.7.2",
    "bootstrap": "^5.3.3",
    "@popperjs/core": "^2.11.8"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.3.0",
    "gh-pages": "^6.3.0"
  }
}
